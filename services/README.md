# Services

Service packages orchestrate domain workflows while depending only on `pkg/*` and `integrations/*` boundaries.

Current planned service set includes the simulation service for synthetic proving-ground runs and policy assurance.
